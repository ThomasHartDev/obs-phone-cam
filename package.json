{
  "name": "obs-phone-cam",
  "version": "0.7.7",
  "private": true,
  "type": "module",
  "description": "iPhone camera and iPad whiteboard as low-latency OBS sources over your local network. No app install, no per-month fee.",
  "bin": {
    "obs-phone-cam": "server.mjs"
  },
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test --test-concurrency=1 test/*.test.mjs",
    "postinstall": "node scripts/setup-mediapipe.mjs"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.35",
    "qrcode": "^1.5.4",
    "selfsigned": "^2.4.1",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "playwright": "^1.48.0"
  }
}
